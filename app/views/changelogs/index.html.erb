<% @logs.each do |log| %>
	<!--TODO: bad code style, needs refactor-->
	<% if log.obj_type == 'Post'%>
		<div class="box post-item">
			<%= image_tag current_user.user_information.avatar.url(:medium), :class=>"post-head" %>
			<%= link_to current_user.user_information.name, profile_path(current_user.id), :class=>"post-username" %>
			<% post = Post.find_by_id(log.obj_id) %>
			<%= image_tag "mood"+post.mood.to_s+".png", :class=>"post-face" %>
			<div class="post-content">
				<p><%= post.message %></p>
			</div>
		</div>
	<% elsif log.obj_type == 'Checkin' %>
		<div class="checkin-item">
			<%= image_tag("checkedin.svg", :class => 'checked-in-icon') %>
			<div class="checkin-content">
				<% c = Checkin.find_by_id(log.obj_id) %>
				<%= link_to log.user.user_information.name, profile_path(current_user.id), :class=>"log-username" %>
					has checked in at <%= c.date %> <%= c.time.in_time_zone('Pacific Time (US & Canada)') %>
			</div>
		</div>
	<%end%>
<% end %>
