<!--%= image_tag "check.png", :id => "check"%-->
<!--h2>整體狀態</h2>
<div class="chart"></div>
<h2>最新動態</h2>
<div class="updates">
	<% recent_changelogs(10).each do |change| %>
		<div>
			<%= change.obj_type %>
			<%= change.message %>
		</div>
	<% end %>
</div-->
<!DOCTYPE html>
<html>
<head>
  <title>Devoke</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= image_tag "intro.jpg", :id => "intro"%>
<% if current_user %>
	<div class="button_area">
		<div class="btn" id="finish">
			<%= link_to 'Finished!', '#' %>
		</div>
		<div id = "welcome">
			<% if current_user.user_information.avatar %>
				<%= link_to(image_tag(current_user.user_information.avatar.url(:medium), :class=>"head"), profile_path(current_user)) %>
			<% else %>
			    <%= link_to(image_tag("fhead", :class=>"head"), profile_path(current_user)) %>
			<% end %>
			<p class="greeting"><%= current_user.user_information.name%>， welcome back!</p>
		</div>
	</div>
<% else %>
    <div class="button_area">
    	<div class="btn" id="signup">
    		<%= link_to('Sign up', new_user_registration_path)%>
    	</div>
    	<div class="btn" id="signin">
    		<%= link_to('Sign in', new_user_session_path)%>
    	</div>
	</div>
<% end %>
</body>